# Minimal RAG Backend Requirements (TF-IDF only, no FAISS)
# Use this if faiss-cpu or sentence-transformers fail to install

# Core backend
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# TF-IDF retrieval (always works)
scikit-learn>=1.3.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0

# Phase 3: Model inference
transformers>=4.36.0
torch>=2.1.0
safetensors>=0.4.0









